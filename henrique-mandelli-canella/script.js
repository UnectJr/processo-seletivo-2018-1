$(document).ready(function(){

            // Init Slider
            $('.slider').slider({height: 500, indicators: false});
            // Init sidenav
            $('.sidenav').sidenav();

            });

